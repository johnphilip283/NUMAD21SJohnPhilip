<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.madcourse.numad21s_johnphilip">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_jp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_jp_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD21SJohnPhilip">
        <activity android:name=".ClickyActivity"></activity>
        <activity android:name=".LinkCollectorActivity"></activity>
        <activity android:name=".LocatorActivity"></activity>
        <activity android:name=".MovieActivity"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.NUMAD21SJohnPhilip.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>